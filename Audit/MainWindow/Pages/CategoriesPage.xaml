<Page x:Class="Audit.MainWindow.Pages.CategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Audit"
      xmlns:collections="clr-namespace:System.Collections;assembly=System.Runtime"
      xmlns:objects="clr-namespace:Audit.Objects"
      mc:Ignorable="d"
      Title="CategoriesPage">
    <Grid Background="Lavender">
        <DataGrid x:Name="CategoriesGrid" AutoGenerateColumns="False">
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="WHITE"/>
                    <Setter Property="BorderThickness" Value="0,1,0,1" />
                </Style>
            </DataGrid.Resources>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Идентификатор"  Binding="{Binding Path=Id}" Width="2*" />
                <DataGridTextColumn Header="Название" Binding="{Binding Path=Name}" Width="6*" />
                <DataGridTextColumn Header="Оплата" Binding="{Binding Path=Payment}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
