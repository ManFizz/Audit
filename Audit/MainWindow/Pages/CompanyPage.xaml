<Page x:Class="Audit.MainWindow.CompanyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Audit.MainWindow"
      mc:Ignorable="d"
      Title="CompanyPage">
    <Grid Background="Lavender">
        <DataGrid x:Name="CompanyGrid" AutoGenerateColumns="False">
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="WHITE"/>
                    <Setter Property="BorderThickness" Value="0,1,0,1" />
                </Style>
            </DataGrid.Resources>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Индетификатор"  Binding="{Binding Path=Id}" Width="2*" />
                <DataGridHyperlinkColumn Header="Название" Binding="{Binding Path=Name}" Width="6*" />
                <DataGridTextColumn Header="Адресс" Binding="{Binding Path=Adress}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
